# Keep this file root-owned with mode 0600. The account is used only while
# applying migrations and should have ALTER, CREATE, INDEX, DROP, SELECT,
# INSERT, UPDATE, and DELETE on the GradTrack schema.
DB_USER=gradtrack_migrator
DB_PASSWORD=replace-with-migration-password
