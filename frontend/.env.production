# Production values are intentionally not committed. Set both variables in the
# deployment build environment; the Vite build fails fast when either is absent,
# localhost, a placeholder, or not TLS-protected.
# VITE_API_BASE_URL=https://grad-track.app/backend
# VITE_REALTIME_URL=https://grad-track.app
